#include "Arduino.h"
#include "SensorArray.h"
#include <DistanceSensor.h>

// SensorArrayStruct ::SensorArrayStruct(int* trigArr, int* echoArr) {
//   sensor1 = DistanceSensor(&trigArr, &echoArr)
// }

 SensorArray ::SensorArray() {
  // trigArr = {5, 7, 9, 11};
  // echoArr = {6, 8, 10, 12};

//  sensorArrayStruct = SensorArrayStruct(trigArr, echoArr);

  // sensorArray[0] = DistanceSensor(trigPinStruct.s1_pin, echoPinStruct.s1_pin);

  // sensor1 = DistanceSensor(trigArr[0], echoArr[0]);
  // sensor2 = DistanceSensor(trigArr[1], echoArr[1]);
  // sensor3 = DistanceSensor(trigArr[2], echoArr[2]);
  // sensor4 = DistanceSensor(trigArr[3], echoArr[3]);
 }